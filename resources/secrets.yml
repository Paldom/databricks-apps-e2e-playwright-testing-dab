resources:
  secret_scopes:
    app_oauth_scope:
      name: "${var.secret_scope_name}"
      permissions:
        - level: READ
          group_name: users
        - level: MANAGE
          group_name: admins
